veRelatório
Tarefa 1 — NÃO aplicada. Antes de mudar Processo.getPareceres()/getAnexos() para Collections.unmodifiableList, rodei o Grep pedido em src/main e src/test (a vistoria só tinha confirmado src/main). Achei que a premissa está errada: pelo menos 6 arquivos de teste chamam .add() diretamente sobre esses getters —

ProcessoControllerEmailTest.java (linhas 76, 221, 379, 401, 407, 479)
ProcessoDetalheControllerTest.java (linhas 252, 253, 267, 269, 270, 291, 292, 296, 298, 300, 317, 336, 349)
Isso é mais de 15 pontos de mutação real via processo.getPareceres().add(x) / processo.getAnexos().add(x). Tornar o getter imutável não quebraria a compilação (o tipo declarado continua List<T>), mas quebraria em runtime com UnsupportedOperationException em todos esses testes. Como meu escopo de arquivos de teste autorizados a editar é só "os que referenciam métodos que eu for remover" (e aqui nada está sendo removido, é mudança de comportamento), não teria permissão para consertar esses 15+ pontos sem exceder o conjunto de arquivos combinado. Deixei o getter como está (retorna a lista mutável direto) e não mexi em Processo.java para essa tarefa. Recomendo, numa sessão futura com escopo maior, migrar esses testes para usar addParecer()/addAnexo() (que já fazem o vínculo bidirecional) antes de endurecer os getters.

Tarefa 2 — Concluída. Confirmado por Grep que getBadgeClasse() só é chamado em sua própria declaração (nenhum uso em templates, controllers ou testes). A única outra ocorrência do nome é uma menção em docs/PLANO-FLUXO.md:30 (documentação, fora do meu escopo de arquivos — deixei intacta, mas fica desatualizada depois desta remoção; vale um ajuste de doc numa sessão futura). Removido o método inteiro de StatusProcesso.java.

Tarefa 3 — Concluída. Confirmado que getProcessoId()/setProcessoId() não são chamados em nenhum service/controller — só existem na própria classe ControleUrgencia. Javadoc reescrito para deixar claro que o campo existe no schema mas hoje não há vínculo automático implementado; é preenchimento manual do operador ou fica nulo.

Tarefa 4 — Concluída (os 4 métodos). Grep individual confirmou zero chamadores para cada um (só a própria declaração):

ProcessoRepository.findAllComPareceres() — removido.
ProcessoRepository.findByStatusOrderByAnoDescSequencialDesc(StatusProcesso) — removido.
ControleUrgenciaRepository.findByAtivoTrueOrderByDataVencimentoAsc() — removido.
ControleUrgenciaRepository.findBySituacaoOrderByDataVencimentoAsc(SituacaoUrgencia) — removido.
Nenhum dos quatro teve uso real que a vistoria anterior tivesse deixado passar.

Compilação: mvn -q -DskipTests compile passou limpo (0 erros). Também rodei mvn -q -DskipTests test-compile para confirmar que as remoções das Tarefas 2-4 não quebraram nenhum teste existente — passou limpo também.

Arquivos alterados:

c:\Users\rafae\projetos\urgencia\src\main\java\br\gov\saude\sgpur\domain\StatusProcesso.java
c:\Users\rafae\projetos\urgencia\src\main\java\br\gov\saude\sgpur\domain\ControleUrgencia.java
c:\Users\rafae\projetos\urgencia\src\main\java\br\gov\saude\sgpur\repository\ProcessoRepository.java
c:\Users\rafae\projetos\urgencia\src\main\java\br\gov\saude\sgpur\repository\ControleUrgenciaRepository.java
Arquivo não alterado (Tarefa 1 pendente, motivo acima):

c:\Users\rafae\projetos\urgencia\src\main\java\br\gov\saude\sgpur\domain\Processo.java



Relatório
Extração feita: os cálculos de gating das abas (liberadoRecebimento/Envio/Respostas/Decisao/Finalizacao) e do sub-rótulo de status (statusSubrotulo) foram movidos de ProcessoDetalheController.detalhe(...) para FluxoProcessoService, dois métodos novos:

FluxoProcessoService.GatingAbas (record) + calcularGating(Processo p) — mesma lógica exata que existia inline (recebimento sempre liberado; envio exige recebimento; respostas exige envio+recebimento; decisão exige respostas OK e não estar em pausa "Solicita informação"; finalização exige status finalizado).
calcularSubrotuloStatus(Processo p) — mesma lógica de texto ("Maioria formada...", "Aguardando parecer (x/total)", "Pareceres recebidos..."), retornando null quando não aplicável.
Escolhi não criar um service novo: FluxoProcessoService já é literalmente descrita no CLAUDE.md como "fonte única" do checklist/wizard, e esses dois cálculos usam exatamente os mesmos dados/condições (mesmos booleans recebimentoFeito, envioFeito, maioriaFormada, respostasOk) que montarEtapas/montarPassosWizard já calculam — mantê-los juntos evita duplicar a regra em dois lugares e reforça a garantia (já documentada) de que timeline, wizard e gating nunca divergem.

Arquivos alterados:

src/main/java/br/gov/saude/sgpur/service/FluxoProcessoService.java — adicionado GatingAbas, calcularGating, calcularSubrotuloStatus.
src/main/java/br/gov/saude/sgpur/web/ProcessoDetalheController.java — detalhe(...) agora chama fluxoService.calcularGating(p) e fluxoService.calcularSubrotuloStatus(p) em vez de recalcular inline.
src/test/java/br/gov/saude/sgpur/web/ProcessoDetalheControllerTest.java — como FluxoProcessoService é @MockitoBean nesse teste, adicionei um stub default em setUp() (calcularGating → nada liberado além do recebimento; calcularSubrotuloStatus → null) e sobrescrevi esse stub nos 3 testes que verificam cenários específicos de gating/subrótulo (detalheLiberaEnvioQuandoRecebimentoFoiFeito, detalheMostraAguardandoParecerQuandoAindaNaoHaMaioria, detalheLiberaDecisaoQuandoMaioriaFormadaESemAnexoPendente). Sem esse ajuste os testes quebrariam com NPE, já que o mock do service não calcula nada sozinho.
Tamanho do método detalhe(...): antes ~144 linhas (165–308 no arquivo original), depois ~106 linhas (165–270). O bloco de ~60 linhas de gating/subrótulo virou 3 chamadas de método + atribuições de model.

Item 5 (MembroUrgenciaRenalRepository injetado direto): confirmado que o controller injeta MembroUrgenciaRenalRepository diretamente (usado em novo() e salvar(), não em detalhe()). Não há hoje nenhum service que encapsule findByAtivoTrueOrderByInstituicaoAsc() — UsuarioController e RelatorioController fazem exatamente a mesma injeção direta do repositório. Como não existe um método de service pronto para substituir com baixo risco, e criar um novo service só para isso seria uma mudança maior que o pedido, deixei como está e documento aqui para uma sessão futura dedicada (afetaria 3 controllers, não só este).

Verificação: mvn -DskipTests compile limpo; mvn -Dtest=ProcessoDetalheControllerTest test — 30 testes, 0 falhas, 0 erros. Não rodei a suíte completa (mvn test), conforme instruído, para não conflitar com outros agentes usando o mesmo target/.

Resumo
Tarefa 1 — Regra de Envio espelhada no service:

ProcessoValidator.validarRegistroEnvio(Processo) novo — mesma checagem (comprovante EMAIL_ENVIADO_AVALIADORES + doc clínico DOCUMENTO_CLINICO_AVALIADOR com contentType PDF).
ProcessoService.registrarEnvio agora chama esse validator antes de trocar o status, lançando IllegalStateException (mesmo padrão de decidir()/confirmarRespostaSolicitante()).
Tarefa 2 — RegistroEnvioService extraído (src/main/java/.../service/RegistroEnvioService.java): toda a lógica de leitura de bytes, validação de PDF com PdfReader (corrompido/sem senha/sem páginas), consolidação, carimbo, salvar-antes-de-remover e auditoria (ANEXO_ADICIONADO/ENVIO_AVALIADORES_REGISTRADO) foi movida do controller para cá, preservando mensagens e comportamento observável. ProcessoDecisaoController.registrarEnvio caiu de ~127 para ~15 linhas: só checa bloqueio por encerramento e mapeia RegistroEnvioResultado para flash erro/msg/aviso.

Tarefa 3 — Acesso a repository removido dos controllers:

ProcessoService ganhou ParecerRepository no construtor + buscarParecer(processoId, parecerId) e pareceresPendentesComEmail(processoId).
ProcessoDecisaoController não injeta mais ParecerRepository nem SolicitacaoAvaliadorService (este último passou a viver só dentro de RegistroEnvioService); os 5 usos (respostaAvaliador, lembreteAvaliador, lembretePendentes, preverEmail x2) agora chamam processoService.buscarParecer(...)/processoService.pareceresPendentesComEmail(...).
AvaliadorController foi deixado intocado de propósito: seu método estático pendentesDoMembro(ParecerRepository, Long) é chamado por GlobalModelAdvice (fora da minha lista de arquivos permitidos), então mudar essa assinatura exigiria editar um arquivo que não estava autorizado a tocar. Da mesma forma, AnexoRepository/UsuarioRepository no AvaliadorController não têm equivalente pronto em AnexoStorageService/UsuarioService para as consultas usadas (findByProcessoIdAndTipo, findByUsername com uso específico) sem risco de mudar semântica. Documentando aqui em vez de arriscar quebra, conforme instruído.
Tarefa 4 — IP na auditoria de decisão: decidir() agora recebe HttpServletRequest e chama auditoria.registrar("PROCESSO_DECIDIDO", detalhe, request.getRemoteAddr()).

Testes ajustados:

ProcessoServiceTest: construtor com ParecerRepository mock; 2 testes novos para registrarEnvio rejeitando sem comprovante/sem doc clínico; teste existente ajustado para anexar os pré-requisitos.
ProcessoDecisaoControllerTest: removido mock de ParecerRepository, adicionado RegistroEnvioService; verificação de auditoria trocada para a variante com IP.
ProcessoControllerEmailTest: trocado parecerRepository/solicitacaoAvaliadorService por processoService.buscarParecer(...)/pareceresPendentesComEmail(...)/registroEnvioService; os 2 testes de "bloqueio sem documento clínico" viraram 2 testes que só verificam o mapeamento erro→flash e sucesso+avisos→flash (a lógica de negócio detalhada agora está em RegistroEnvioServiceTest).
AvaliadorControllerTest: sem alterações (não precisou).
Novo RegistroEnvioServiceTest: sucesso normal, PDF corrompido ignorado com aviso mas envio segue (havia outro válido), bloqueio sem nenhum PDF válido, bloqueio sem comprovante de envio, bloqueio quando todos os PDFs estão corrompidos.
Fallout não previsto: FluxoProcessoServiceTest (fora da minha lista) quebrava a compilação por instanciar ProcessoService diretamente com 3 argumentos; ajustei o mínimo necessário (adicionar mock de ParecerRepository e passar no construtor) para não travar o build de todo mundo.
Compilação (mvn -DskipTests compile e test-compile) limpa, 0 erros. Não rodei mvn test conforme instruído.

Todos os 4 agentes terminaram. Agora vou rodar a suíte completa para validar a integração de todas as mudanças.