# Histórico (arquivo morto)

Notas de sessão e relatórios de vistoria antigos, movidos da raiz do repositório
em 2026-07-29 para desentulhar o diretório principal.

**Isto não é fonte da verdade.** O conteúdo relevante já foi absorvido no
`CLAUDE.md`. Vários arquivos aqui têm encoding corrompido (mojibake) desde a
época em que foram criados e descrevem estados do sistema que já mudaram —
consulte-os só para arqueologia, nunca para decidir comportamento atual.

| Arquivo | O que é |
|---|---|
| `sessao-2026-07-27-resumo.md` | Resumo da sessão de 27/07/2026 (etapas 5–6 automatizadas, portal do solicitante). Antes era `.sessao-resumo.md` na raiz. |
| `vistoria-pendente.md` | Stub truncado (só o título, 9 bytes). Antes era `.vistoria-pendente.md`. |
| `relatorio-vistoria-limpeza-codigo.txt` | Relatório de uma vistoria de código morto (getters imutáveis, métodos de repositório sem uso). Antes era `Relatório.txt`. |
| `nota-modulo-solicitante.txt` | Nota ditada pelo usuário com a ideia original do módulo do solicitante. Antes era `relatorios veja claude.txt`. |
